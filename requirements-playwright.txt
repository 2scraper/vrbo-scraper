# The recommended engine. After installing, fetch the browser itself — and
# note WHICH browser:
#
#   pip install -r requirements.txt -r requirements-playwright.txt
#   playwright install chrome
#
# `chrome` rather than `chromium`, and worth doing — a real browser is the
# more faithful client. It is NOT the difference between working and not:
# measured 2026-09-14, the two builds interleaved on a clean address, six
# requests each, 12 of 12 served. What decides access is how the exit address
# is scored. An earlier version of this file claimed otherwise on one sample
# per arm.
#
# `playwright install chrome` installs Google Chrome through the system
# package manager, so it needs the same privileges any other browser install
# would. If that is not available, `--browser-channel msedge` also drives a
# real browser; the bundled chromium is the one to avoid.
playwright>=1.40.0
